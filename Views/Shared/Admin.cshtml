@{
    ViewData["Title"] = "Admin";
}

<h3>@ViewData["Title"]</h3>

<a class="nav-link" asp-area="Identity" asp-page="/Account/Register">Register a new researcher</a>

<a class="nav-link" asp-controller="Administration" asp-action="ManageUsers">Manage existing users</a>

@*<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <td>Id</td>
            <td>Name</td>
        </tr>
    </thead>
    <tbody>
        @foreach (var role in ViewBag.roles)
        {
            <tr>
                <td>@role.Id</td>
                <td>@role.Name</td>
            </tr>

        }
    </tbody>
</table>*@






